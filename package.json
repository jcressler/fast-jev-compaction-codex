{
  "name": "fast-jev-compaction-codex",
  "version": "0.3.2",
  "description": "Task-aware Jev evidence selection and exact local recovery around native Codex compaction.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.12"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jcressler/fast-jev-compaction-codex.git"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "fast-jev-codex": "./dist/cli.js"
  },
  "files": [
    "dist",
    "hooks",
    "skills",
    ".codex-plugin",
    "README.md",
    "NOTICE",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.tests.json",
    "test": "vitest run",
    "demo": "node examples/demo.mjs",
    "benchmark": "node benchmarks/replay.mjs && node benchmarks/score-self-test.mjs && node benchmarks/jev-trial-self-test.mjs && node benchmarks/long-task-fixture.test.mjs && node benchmarks/batched-search.mjs",
    "check": "npm run typecheck && npm test && npm run build",
    "prepack": "npm run build",
    "trial:jev": "node benchmarks/jev-trial.mjs --live --allow-network",
    "trial:long": "node benchmarks/long-task.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^5.0.1"
  }
}
