interface:
  display_name: "Fast Jev Compaction"
  short_description: "Recover local evidence after native compaction"
  default_prompt: "Use $fast-jev-compaction to search or retrieve a user-authorized local Codex recovery archive."
